const initialState = {
    pokemons: [],
    filteredPokemons: []
}

export default function reducer( state = initialState, action){
    switch (action.type) {
        case value:
            
    
        default:
            return state;
    }
}